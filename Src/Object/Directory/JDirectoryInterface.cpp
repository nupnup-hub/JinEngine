#include"JDirectoryInterface.h"

namespace JinEngine
{
	JDirectoryOCInterface::JDirectoryOCInterface(const std::string& name, const size_t guid, const J_OBJECT_FLAG flag)
		:JObject(name, guid, flag)
	{}
}